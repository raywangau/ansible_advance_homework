---
# tasks file for roles/osp-instance-delete
- name: Fetch Instance Info
  os_server_facts:
    cloud: ospcloud
    region_name: RegionOne
  register: result
  tags:
    - instance-info
    - instance-stop

- name: Delete instances
  os_server:
    cloud: ospcloud
    server: "{{ item.name }}"
    state: absent
    wait: yes
  loop: "{{ instanceinfo.openstack_servers }}"
  tags: instance-stop
